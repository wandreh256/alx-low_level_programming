#include <unistd.h>

/**
 * _putchar - write the character c to stdout
 * @x: The character to print
 * Return 1 if true, 0 if false
 * On error, -1 return.
 */
int_putchar(char)
{
return (write(1, & c, 1));
}
